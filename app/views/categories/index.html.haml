.row
  .col-lg-4
    %span.header Категории
  .col-lg-4
    = link_to [:new, :category], class: 'btn btn-primary' do
      .glyphicon.glyphicon-plus{ 'aria-hidden' => "true" }
      = t('new')
.row
  - if @categories.any?
    %table.table
      %thead
        %tr
          %th= Item.human_attribute_name(:name)
          %th
      %tbody
        - @categories.all.each do |category|
          %tr
            %td= link_to category.name, [:edit, category]
            %td
              .btn-group
                = link_to t('short_edit'), [:edit, category], class: 'btn btn-default btn-sm'
                = link_to [category], method: :delete, data: { confirm: t('delete_confirmation') }, class: 'btn btn-danger btn-sm' do
                  %span.glyphicon.glyphicon-remove= t('delete')
  - else
    %h2 Вы пока не добавили ни одной категории
    %p
      Начните с добавления корневых категорий. Затем добавьте наследников
    = link_to [:new, :category], class: 'btn btn-primary' do
      .glyphicon.glyphicon-plus{ 'aria-hidden' => "true" }
      = t('new')
